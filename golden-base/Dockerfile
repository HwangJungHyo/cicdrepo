# =============================================================================
# [실험용] 스캔 게이트 검증
# 의도적으로 취약한 베이스를 사용해 파이프라인이 차단하는지 확인한다.
# 이 커밋은 절대 merge하지 않는다. PR의 CI 결과만 확인한다.
# =============================================================================

FROM ubuntu:20.04

LABEL org.opencontainers.image.title="golden-base-VULNERABLE-TEST"
